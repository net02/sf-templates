{% extends "::base.html.twig" %}

{% set menu = knp_menu_get('main') %}
{% block body %}
<style type="text/css">
    li.current > a { font-weight: bold; }
</style>
    {# knp_menu_render('main') #}
    {{ knp_menu_render(menu) }}

    {{ render(controller('AcmeDemoBundle:Demo:switch', {'current': menu.currentItem })) }}
{% endblock %}
